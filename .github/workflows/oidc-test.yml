name: Deploy Todo Application

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:

      # 1. Checkout deployment repository
      - name: Checkout deployment repository
        uses: actions/checkout@v4

      # 2. GitHub OIDC → AWS STS → IAM Role
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::109684670986:role/GitHubActionsSSM
          aws-region: ap-south-1

      # 3. Verify that GitHub successfully assumed the AWS role
      - name: Verify AWS identity
        run: |
          aws sts get-caller-identity

      # 4. Send deployment command to EC2 through SSM
      - name: Deploy on EC2
        run: |
          COMMAND_ID=$(aws ssm send-command \
            --instance-ids i-0ef3723e1f90886fd \
            --document-name "AWS-RunShellScript" \
            --parameters 'commands=[
              "cd /home/ubuntu/todo-deployment",
              "./deploy.sh"
            ]' \
            --query "Command.CommandId" \
            --output text)

          echo "COMMAND_ID=$COMMAND_ID" >> "$GITHUB_ENV"

          echo "SSM Command ID: $COMMAND_ID"

      # 5. Wait for SSM command to finish
      - name: Wait for deployment
        continue-on-error: true
        run: |
          aws ssm wait command-executed \
            --command-id "$COMMAND_ID" \
            --instance-id i-0ef3723e1f90886fd

      # 6. Always get the actual SSM result
      - name: Get deployment result
        if: always()
        run: |
          aws ssm get-command-invocation \
            --command-id "$COMMAND_ID" \
            --instance-id i-0ef3723e1f90886fd \
            --query '{
              Status: Status,
              StatusDetails: StatusDetails,
              StandardOutput: StandardOutputContent,
              StandardError: StandardErrorContent
            }' \
            --output json

      # 7. Fail the GitHub job if deployment failed
      - name: Check deployment status
        if: always()
        run: |
          STATUS=$(aws ssm get-command-invocation \
            --command-id "$COMMAND_ID" \
            --instance-id i-0ef3723e1f90886fd \
            --query 'Status' \
            --output text)

          echo "Deployment status: $STATUS"

          if [ "$STATUS" != "Success" ]; then
            echo "Deployment failed."
            exit 1
          fi

          echo "Deployment successful."
